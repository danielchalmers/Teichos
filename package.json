{
  "name": "teichos",
  "version": "0.0.0",
  "description": "A web extension to block URLs based on configurable regex filters with time-based scheduling",
  "type": "module",
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "scripts": {
    "dev": "tsx scripts/build.ts --watch --dev",
    "build:dev": "tsx scripts/build.ts --dev",
    "build": "tsx scripts/build.ts",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "playwright:install": "playwright install --with-deps chromium",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc -p tsconfig.test.json --noEmit",
    "check:fast": "npm run format:check && npm run lint && npm run typecheck && npm run typecheck:test && npm run test:unit",
    "check": "npm run check:fast && npm run build && npm run test:e2e",
    "clean": "rimraf dist",
    "package": "npm run clean && npm run build && cd dist && zip -r ../teichos.zip ."
  },
  "keywords": [
    "webextension",
    "browser-extension",
    "url-blocker",
    "chrome-extension",
    "edge-extension"
  ],
  "author": "Daniel Chalmers",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.59.1",
    "@types/chrome": "^0.1.42",
    "@types/node": "^25.6.2",
    "@vitest/coverage-v8": "^4.1.5",
    "esbuild": "^0.28.0",
    "eslint": "^10.3.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.3",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.2",
    "vitest": "^4.1.5"
  }
}
